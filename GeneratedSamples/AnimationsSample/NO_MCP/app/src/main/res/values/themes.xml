<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AnimationsSample" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@android:color/holo_purple</item>
        <item name="android:colorPrimaryDark">@android:color/holo_purple</item>
        <item name="android:colorAccent">@android:color/holo_purple</item>
        <!-- Customize your theme here. -->
    </style>
</resources>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- This intent filter indicates that the app can be launched in immersive mode. -->
            <intent-filter>
                <action android:name="com.meta.xr.intent.action.LAUNCH" />
                <category android:name="com.meta.xr.intent.category.IMMERSIVE" />
            </intent-filter>
            <meta-data
                android:name="com.meta.xr.hybrid.app.mode"
                android:value="hybrid" />
        </activity>

        <meta-data
            android:name="com.meta.xr.supportedDevices"
            android:value="quest2|questpro|quest3" />

    </application>

    <!-- Required VR features -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" />
    <uses-feature android:name="com.meta.xr.feature.PASSTHROUGH" android:required="true" />

</manifest>
